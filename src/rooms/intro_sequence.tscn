[gd_scene load_steps=2 format=3]

; "The First Descent" backstory crawl. A one-node stub: the script builds the
; whole UI in code (background, title, beat text, prompt line), so there is no
; layout to keep in sync here. Routed to from the title for fresh players; hands
; off to tutorial_run.tscn on the last beat or a skip.

[ext_resource type="Script" path="res://src/rooms/intro_sequence.gd" id="1_intro"]

[node name="IntroSequence" type="Control"]
layout_mode = 3
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
grow_horizontal = 2
grow_vertical = 2
script = ExtResource("1_intro")
