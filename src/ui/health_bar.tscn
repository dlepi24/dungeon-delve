[gd_scene load_steps=2 format=3]

; Draw order matters: background, then the slow red drain, then the green fill on
; top. The red is only ever visible where the green has receded — that sliver IS
; the readout of how hard you just got hit.

[ext_resource type="Script" path="res://src/ui/health_bar.gd" id="1_bar"]

[node name="HealthBar" type="Node2D"]
script = ExtResource("1_bar")

[node name="Background" type="ColorRect" parent="."]
offset_right = 46.0
offset_bottom = 6.0
color = Color(0, 0, 0, 0.65)

[node name="Drain" type="ColorRect" parent="."]
offset_right = 46.0
offset_bottom = 6.0
color = Color(0.9, 0.25, 0.25, 1)

[node name="Fill" type="ColorRect" parent="."]
offset_right = 46.0
offset_bottom = 6.0
color = Color(0.35, 0.85, 0.4, 1)
