# Game Design Document

Living document. Decisions get made in design sessions, then land here. The decision log at the bottom is append-only.

## Pitch
A 2D side-view action roguelite. You delve into procedurally assembled depths, fight with tight skill-based combat, gather resources, and decide how greedy to get before extracting. Back at a persistent hub, you spend the haul on gear and upgrades, then go deeper. Dead Cells combat feel, extraction greed tension, Spelunky-style daily-seed competition.

## Design pillars
1. **Tight, fair, readable combat.** Every death feels earned. Responsiveness at Celeste/Dead Cells standard.
2. **Greed tension.** Extract safe or push deeper is the core decision of every run.
3. **Mastery is visible.** A skilled run looks different from an average run. Parry-heavy play is the skill ceiling.
4. **Runs fit a session.** 20 to 40 minutes, with a deliberate one-more-run pull.
5. **Progress persists.** The hub grows even when runs fail.

## Locked decisions

### Perspective and genre
- Side-view platformer-action roguelite. Platforming skill is part of combat expression (spacing, verticality, air control).

### Input
Controller-first design discipline. Budget is ~10 verbs. No new core-combat verbs without a decision log entry.

| Action | KB+M (locked) | Gamepad (provisional, tune in playtest) |
|---|---|---|
| Move | WASD | Left stick or D-pad |
| Aim up / ladders | W | Stick up or D-pad up |
| Jump | Space | A / Cross |
| Dodge roll | Shift | B / Circle |
| Primary attack | LMB or J | X / Square |
| Parry | RMB or K | Y / Triangle |
| Skill 1 (weapon swap for now) | Q | LB |
| Skill 2 (timber hook) | E | RB |
| Interact | F | A / Cross |
| Consumables | 1 / 2 | D-pad left / right |

Consumables are never core combat. All bindings go through Godot InputMap actions so devices are interchangeable.

### Combat rules
- **Roll:** safe, always available, i-frames on good timing, never punished. No stamina in v1. Add a light stamina cost only if roll spam proves degenerate in playtesting, and only via a decision log entry.
- **Poise (enemy hyper armor):** enemies flinch freely when idle, walking or recovering, but once a telegraph begins they have poise for the duration of that attack. Poise is per-attack: a heavier swing carries more. Chip it below zero and they are knocked off balance; fail to, and they swing through your pokes and hit you. Poise refills between attacks.
  - A **poise break** staggers them. It does NOT open a riposte — that is parry's reward alone, or parry stops being the greedy option and becomes a worse version of attacking.
  - A **parry** always breaks poise outright, whatever the value. Parry is the answer to a heavy attack you cannot poke through.
  - Exists because attack-spam beat everything: every hit cancelled the enemy's telegraph, so parry was never worth its risk. That is a pillar violation, not a balance nit.
- **Parry:** design pillar. Tight active window, deflects the incoming attack, opens a riposte. Whiffing has recovery frames and is punishable. Reward is speed and style. Roll-only play must remain fully viable.
- **Attacks:** animation commitment (weight) with defined cancel-into-roll windows. Where those windows sit is a primary tuning knob.
- **Enemies:** telegraph everything. Readability over surprise.

### Feel spec (starting values, Dustin tunes everything in-editor)
| Parameter | Start value | Notes |
|---|---|---|
| Physics tick | 60/s fixed | Gameplay in `_physics_process` only |
| Input buffer | 100 ms | Queued inputs fire when legal instead of dropping |
| Coyote time | 80 ms | Jump remains valid briefly after leaving a ledge |
| Jump | Variable height | Release early to cut the jump |
| Roll | ~350 ms total | I-frames cover roughly the middle 200 ms |
| Parry active window | 120 ms | Whiff recovery ~300 ms |
| Hitstop | 3 frames normal hit, 6 on parry | Half of "crunchy" lives here |

Screenshake, squash/stretch, and flashes arrive in Milestone 2.

### Competition model
- No real-time multiplayer, period. Netcode is out of scope.
- Async competition: daily seed runs, leaderboards, ghost replays. Local first, online later if wanted.
- Determinism requirements: seeded RNG through one central service, all gameplay on the fixed tick, ghosts stored as recorded inputs.

### Content architecture
- Data-driven via custom Resources (`.tres`): weapons, enemies, upgrades, rooms.

### Collision layers (initial, may extend in M1)
| # | Name |
|---|---|
| 1 | World |
| 2 | Player |
| 3 | Enemy |
| 4 | PlayerAttack |
| 5 | EnemyAttack |
| 6 | Pickup |

### Setting (locked 2026-07-15)
**A collapsing mine. You go down for ore and relics while the shaft fails behind you.**

Why this and not the alternatives: it makes the greed pillar *literal* rather than
abstract. Deeper is richer and less stable, so "extract or push on" is a fact of
the fiction and not a rule bolted onto it. It also fits the Spelunky-style
daily-seed DNA the GDD already commits to.

What it settles:
- **Why you go:** the haul. Ore, relics, whatever is worth carrying up.
- **Why you leave:** the mine is failing. Depth is the timer.
- **Visual language:** rock, timber supports, ore veins, lantern light against
  cold dark. Warm = yours and safe; cold = the mine. Amber = value.
- **What the enemies are:** the LOST CREWS — miners who never came back up.
  Settled 2026-07-21 (see decision log); the woken-things/fauna direction was
  rejected.

Still open, and NOT implied by this: death and extraction rules (question 2), the
meta shape (question 3), and the name (question 5). Locking the theme does not
lock those.

### Run structure
- Delve levels are procedurally assembled from hand-built rooms (rooms first, assembly logic later).
- Persistent hub between runs.
- An extraction decision exists mid-run.

### Death, extraction and meta (locked 2026-07-15)
The greed loop, in three decisions:

- **Haul.** You gather haul (ore, relics) as you descend. It is *carried*, not yet
  yours. Enemies drop it; the mine holds it.
- **Extract or descend, every room.** Each room's exit is the decision point. Going
  UP (to the surface) extracts: your carried haul is banked and the run ends safe.
  Going DOWN descends to the next room, richer and more dangerous. The choice is in
  your face at every exit — that IS the greed tension, and up/down maps to the mine
  fiction so it needs no explaining.
- **Death costs everything carried.** Die in the mine and you lose all carried haul.
  Only banked haul is yours. This is the sharp version on purpose: every room deeper
  is more at risk, and the collapsing shaft sells the stakes. (If playtesting proves
  it too punishing, a keep-a-fraction softening is the pre-agreed fallback — decision
  log entry required.)
- **The hub is a vendor.** Banked haul buys permanent stat upgrades that stack across
  runs — so the hub grows even when runs fail (pillar #5). M5 ships ONE upgrade (max
  health) to prove the loop; the rest is M6 content. Not crafting, not base-building
  in v1.

## Open questions
1. ~~**Theme and setting.**~~ **ANSWERED 2026-07-15 — see Setting.**
2. ~~**Death and extraction rules.**~~ **ANSWERED 2026-07-15 — see Death/extraction/meta.**
3. ~~**Meta progression shape.**~~ **ANSWERED 2026-07-15 — vendor with permanent upgrades. See above.**
4. **V1 scope line.** What ships in the first itch.io build.
5. ~~**Name.**~~ **ANSWERED 2026-07-28 — PAYDIRT.** The title screen's working
   title is now the real one (Dustin's call). Landed in `project.godot`
   (config/name) and every export path / bundle id (`us.leapit.paydirt`).

## Decision log
- 2026-07-14: Genre locked: 2D delve-and-return action roguelite. Engine: Godot 4 + GDScript, static typing.
- 2026-07-14: Side-view locked over top-down. Platforming as combat expression.
- 2026-07-14: Input budget locked at ~10 verbs, controller-first discipline. KB+M layout locked, gamepad defaults provisional.
- 2026-07-14: Roll is never punished, no stamina in v1. Parry locked as the greedy pillar with riposte reward.
- 2026-07-14: Competition is async only: daily seeds, leaderboards, ghost replays. Real-time PvP permanently out of scope.
- 2026-07-14: Gray-box before art. Feel stack (buffer, coyote, hitstop, cancel windows) mandatory from Milestone 1.
- 2026-07-15: **Poise / hyper armor added**, Dustin's call after playtesting M4. Enemies keep swinging through pokes during a committed attack; light enemies have little poise, heavy ones a lot. Poise applies ONLY during telegraph/attack, so enemies still flinch when idle and combat stays at the Dead Cells pace rather than drifting to Dark Souls weight. A poise break staggers but gives no riposte; only a parry does. Reason: attack-spam trivially beat everything, which made parry decorative and broke its pillar status.
- 2026-07-15: Enemy attacks are data (`EnemyAttackData` resources) chosen by range, not one hard-coded swing. Folds the Dart's lunge in as a `dash_speed` value rather than a subclass — it turned out to be a number, not a verb. Adding an attack or an enemy variant is now a resource file.
- 2026-07-15: **Setting locked: a collapsing mine.** Dustin's call. You delve for ore and relics while the shaft fails behind you. Chosen because it makes the greed pillar literal — deeper is richer and less stable — rather than a rule sitting on top of an unrelated fiction. Unblocks art, which had been gated on this since M2's gate passed. Does not settle extraction, meta shape or the name.
- 2026-07-15: Key rebinding pulled forward from M7, Dustin's call after finding J/K awkward. The GDD's KB+M table stays the DEFAULT rather than the only option, so the locked layout is now a starting point players can override.
- 2026-07-15: **Run loop locked (M5), Dustin's call.** Gather carried haul in the delve; each room's exit offers extract (go UP, bank haul, end run safe) or descend (go DOWN, richer + deadlier). Death loses ALL carried haul — only banked is kept. Hub is a vendor selling permanent stacking stat upgrades; M5 ships one (max health). Chosen for the sharpest greed tension, which the collapsing-mine fiction already supports. Keep-a-fraction death softening is the pre-agreed fallback if it proves too punishing.
- 2026-07-17: **In-run 2-weapon loadout, Dustin's call (M7).** You hold up to two FOUND weapons and swap live; a pickup fills an empty slot or replaces the one you are NOT holding. Weapons stay run-scoped — lost on death or extract — so the roguelite model is unchanged. The existing skill_1/skill_2 actions (Q/E, LB/RB) are REPURPOSED as the two weapon slots rather than adding a new verb, keeping the ~10-verb budget intact; if real skills ever ship, weapon-swap needs its own binding and a fresh decision here. Swapping is blocked mid-swing on purpose: attack timings are read live, so a swap during recovery would act as a swap-cancel and erase the commitment attacks are designed to carry.
- 2026-07-17: **Persistent meta stats (M7).** GameState tracks total runs, deepest room reached, best single extract and total kills, saved with the meta save and wiped by New Game. Shown on the title screen — the game should remember you played it.
- 2026-07-17 (later): **Weapon swap is ONE key, Dustin's call after playing it.** Two slot-select keys read as nothing until a second weapon existed. Now skill_1 (Q) toggles between the two held weapons, Dead Cells backpack-style; skill_2 (E) returns to being a free slot for a future skill. HUD shows the loadout as a large active-weapon square with the secondary nested behind it.
- 2026-07-17 (later): **Weapons are SESSION-scoped, not run-scoped — Dustin's call after playing the loadout.** Coming out of the mine alive banks your weapon loadout like your haul: extract, shop, descend again, still armed. Death loses the loadout with everything else, and so does quitting the app (the stash is memory-only, never saved). Reason: losing a found Maul as the *reward for successfully extracting* punished the safe choice and fought the greed pillar. Permanent weapon ownership stays a non-goal; that pressure is routed to the blacksmith instead.
- 2026-07-17 (later): **Second hub vendor: the Blacksmith, Dustin's call.** Sells weapons for banked haul; the rack REROLLS a random subset of the weapon pool every visit, so the smithy is worth checking between runs and a run can start armed instead of gambling on drops. Bought weapons use the identical equip path as found ones — session-scoped, lost on death — so the smith sells a head start, not property. Stock rolls are plain (unseeded) randomness: hub flavour must not burn draws from the delve's seeded streams. Shields / parry gear came up in the same note but is NOT designed — needs a session before anything is built.
- 2026-07-17 (later): **Miniboss: the Overseer guards the deep room.** Dustin asked for a bigger end-of-run challenge as meta upgrades outpace the roster. Pure data (a new EnemyStats .tres + an `E` room glyph) per the content rule. Kit is the poise system examined: a 140-poise crush you must parry or roll, a long quake that punishes camping at mid range, and a charge that answers sniping. 340 HP, dies rich (guaranteed-ish heart, 50/50 buff and weapon, 30 ore). The Dart was cut from the deep room — a lunging add on top of a charging boss crossed from pressure into chaos. Deliberately NOT built yet, needs Dustin's call: depth-scaling enemy stats, and random elite variants of normal enemies in mid rooms.
- 2026-07-17 (round 3): **The Overseer is named — Varok, the Overseer — with a Souls-style boss bar.** Playtest: an unnamed miniboss read as another brute. EnemyStats gains `is_boss`; engaging a boss raises a wide named health bar bottom-centre (Events.boss_engaged). A boss announces itself once per fight, not on every re-aggro.
- 2026-07-17 (round 3): **Variety pass, Dustin's call ("same exact levels all day").** Middle-room pool widened 4 -> 7 (cavern, shaft, gallery). Spawn markers demoted from facts to weight-class suggestions: a seeded stream (Rng.stream("spawns")) may swap grunt<->dart and promotes toward brutes as depth rises. Entry room and the boss are exempt — the gentle opening and the guaranteed Overseer are promises. Determinism preserved: same seed, same monsters, so daily seeds stay fair.
- 2026-07-17 (round 4): **Shrines locked, Dustin's calls in session.** One altar-style shrine offers one bargain, rest-of-run duration (timed power stays the buffs' job), refusable free, STACKING if multiple are found. All three flavours ship in v1: stat trades, pay-carried-ore (converts at-risk loot into strength — the greed pillar in miniature), and curse trades (bane is a rule: harder spawns). Shrine spots are `S` glyphs in room ASCII; the delve lights ~1 per run from the seeded stream so daily seeds see identical offers. Pick-one-of-two offers are a possible later extension of the same data.
- 2026-07-17 (round 4): **Run history logged silently from now on.** Every finished run appends {date, seed, outcome, amount, room, kills} to user://run_history.jsonl (capped at 200, wiped by New Game). No UI reads it yet, deliberately — Dustin judged leaderboards premature, but records not written now can never be ranked later. M8's leaderboard/daily mode will read this file.
- 2026-07-17 (round 5): **Mine heat locked, Dustin's call ("shouldn't be able to roflstomp").** Every extraction survived heats the mine: enemies gain health (+12%/level) and damage (+10%/level), spawns promote toward brutes more often (+5%/level), capped at heat 8. Not punishing by design: heat also pays (+8% ore/level) and clearing all five rooms lands a +50% full-clear bonus, so the greed ceiling out-pays the caution floor. Death cools heat to zero — the streak itself becomes something you are afraid to lose, same emotional shape as carried haul. Persists in the save (it tracks the persistent upgrades it exists to counterweigh).
- 2026-07-17 (round 5): **Blacksmith round 2.** Stock rolls once per surface visit (the close/reopen reroll exploit is dead), and the smith gains a trade: HONING the weapon in hand (+15% damage/poise per level, escalating cost, renames to "+N"). Honing mutates a session duplicate — the .tres on disk is shared by every future drop and must never change; weapon_test pins that. Shop UX: F and ESC close the stalls, walking away closes them.
- 2026-07-17 (round 6): **Gamepad menu navigation wired.** Gameplay bindings existed since M0 (the provisional table); what was missing was the menu layer. Every menu now grabs focus on open, D-pad/stick navigates, A accepts, and B (ui_cancel) backs out one level — shops, settings->keybinds->host, pause, and the title wipe-confirm. The seed field is click-to-edit only so stick navigation cannot get trapped in it. Untested on hardware (no controller on hand); glyph art and the gamepad table's final layout stay open until Dustin plays it on a pad.
- 2026-07-17 (round 7): **Pad select fixed + device-aware hints.** Root cause of "can navigate but not select": Godot's DEFAULT ui_accept carries no joypad button (Enter/Space only; ui_cancel is Escape only) while ui_up/down do carry D-pad/stick — so menus half-worked on a pad. project.godot now overrides ui_accept (+A/Cross) and ui_cancel (+B/Circle), pinned by the check gate. On-screen hints are now device-aware: Keybinds tracks the last active device and hint_for() serves keyboard letters or pad glyphs (Xbox letters vs Sony shapes, sniffed from the controller name), flipping live. Text glyphs only — icon art is M9's. Still untested on hardware.
- 2026-07-17 (round 8): **Interact moved to A/Cross on pad (D-pad up kept as secondary), Dustin's call.** One button now means "engage": A interacts in the world, accepts in menus, dismisses results — the up/F split he could not parse is gone. Known accepted quirk: A near a stall also fires jump (both actions own the button; a hop while the shop opens is harmless in the safe hub). Fixed alongside: on pad, D-pad up inside a shop was BOTH navigation and interact, so browsing closed the shop — close-on-interact now excludes presses that double as ui verbs. Sony pads show shape glyphs (✕ ○ □ △); Xbox keeps letters. Music mix: per-context attenuation — title at full bed, hub -5 dB, delve -8 dB (exports on Hub/RunCoordinator).
- 2026-07-17 (round 9, the variety session — all four tracks Dustin's picks): **(1) Projectiles + ranged enemies.** Projectile extends Hitbox so the combat contract holds: rolls dodge rocks, and a PARRY reflects them — faster, double damage, guaranteed stance break. Ranged attacks are data (projectile_speed); flight is data (can_fly + hover_offset). The Slinger breaks tier-camping from across the room; the Gnat contests the high ground with paper health. Both enter via seeded spawn variation as same-weight alternates — never in the entry room. **(2) Branching descent.** Every middle depth seeds TWO candidate rooms; descending offers the doors with flavour hints (a hint, not a name). Choice writes into the run plan; same seed = same doors for everyone. **(3) FTUE.** First-run verb signs in the entry room, hub Training post into the gym (parry signage + return), result screens explain the economy, one-time swap-key toast. **(4) Juice.** Pad rumble laddered like hitstop, room-entry fades, camera dust, hub lantern flicker. Deferred from this conversation: mine hazards (crumbling platforms/spikes/debris) and data-cheap elite variants — next variety batch.
- 2026-07-17 (round 10): **Flinch fatigue — Dustin's fresh call on the deferred stunlock item.** Chaining flinches between attacks stunlocked everything heavier than a grunt. Only `flinch_limit` hits may interrupt per `flinch_window_ms` (default 4/1800; Brute and Overseer 2); further hits deal full damage but the enemy keeps acting. First hits always flinch, so "flinch freely when idle" holds — this kills only the infinite chain. Parry-stagger untouched. enemy_test pins it.
- 2026-07-17 (round 10): **Three delve music moods** (parameterized generator: original 96 BPM, moodier i-iv-v-VI at 84, driving i-VII-VI-v at 108), one drawn per descent with plain (unseeded) randomness — ambience never touches the seeded streams. **Mine lighting**: CanvasModulate darkness (mild — telegraph colours must survive), procedural lantern on the player, shrine altar lights, vignette; all runtime gradients, zero assets, everything exported. Real art direction (packs vs artist vs AI-generated) is still M9's decision — options laid out for Dustin.
- 2026-07-17 (round 11): **Weapon pickup rules locked, Dustin's call ("I keep losing my upgraded weapons").** Auto-equip only from the bare pickaxe. A free second slot auto-STOWS quietly — walking over loot never switches your hand. With a FULL loadout, a dropped weapon stays on the ground as an offer: "[A] Take X — drops Y", trading only on a deliberate interact and always replacing the STOWED weapon, never the hand. Honed blades can no longer be vacuumed away mid-fight. weapon_test pins all three paths.
- 2026-07-17 (round 12): **Pickup floor probe** — pickups (Areas) never collided with the world; the magnet masked it until full-loadout weapons stopped magneting and fell through the floor. Every falling pickup now settles on a ground raycast. **Auto-pause on controller disconnect** (skipped when the tree is already paused). **Juice round two**: roll afterimages + roll/landing dust, enemy death bursts in body colour, lantern flicker. All visual-only, unseeded, self-freeing.
- 2026-07-17 (round 13, "feel like an actual world"): **Camera zoomed to 1.45 and clamped to per-room bounds** — the world scrolls under you now (zoom_level export on FollowCamera; hub stays a wide diorama on purpose). **Room width is free**: the generator derives each room's size from its ASCII, and HALLS (116 cols, first double-wide) is in the pool. Height stays fixed at 18 — the jump-tier budget depends on it. **Music is a mix**: boss vamp on engage (returns to the interrupted mood on death/exit), 35% chance to drift delve variants per room change. OPEN DESIGN QUESTION for a session, Dustin's call: the traversal mechanic for bigger levels (Ori/Celeste-class movement) — candidates: flip allow_air_roll (the export exists, M1 open question), wall-slide+jump, or a Celeste-style air dash. Bigger rooms make this decision due soon.
- 2026-07-17 (round 14): **Every run guarantees ONE big room** (seeded depth, never branched — the centrepiece is not dodgeable). BIG_POOL is its own pool: halls + the new UNDERCROFT (double-wide, two storeys: covered lane below, walkable roof above, exit off the roof — the most Celeste-shaped room yet). Dustin's "I don't see a new level": one-in-three odds read as never; a guarantee reads as design.
- 2026-07-17 (round 15, the wow pass): **Parry shockwave + camera zoom-punch** on parry/riposte — pillar #3 says mastery is visible; now the game's most skilled input detonates. **Room-clear payoff**: new room_cleared event when the last living enemy in a room falls — gold CLEAR call, and every loose pickup vacuums to the player (weapon trade-offers exempt). The end of a fight is a shower of earnings.
- 2026-07-17 (round 16, the hazards batch — deferred pick from the variety session, now built): **The mine itself is a combatant.** Spikes (`v` glyph) line pit floors — survivable-but-costly keeps the no-death-pits rule; unparryable (new `parryable` flag on Hitbox — you cannot deflect the floor) but roll i-frames cross clean, so the roll pillar holds. Crumbling bridges (`C`) judder and drop a beat after contact, re-form later. Debris rain: seeded ceiling rocks, glowing-column telegraph ("telegraph everything" includes the ceiling), count scales with depth + heat ("heat shakes the mine loose"); the rock is a Projectile so a parry bats it away. Entry room never rains.
- 2026-07-18: **TRAVERSAL VERB LOCKED: the Pick Pogo, Dustin's call** (chosen over wall-kick and an anchored timber grapple in a design session). Down+attack in the air strikes beneath you; any connection — enemy, boss, spikes from above, falling debris (destroyed) — launches you past jump height, air control refreshed. Rationale: most thematic (the mining tool IS the movement), zero input-budget cost, needs a target so tier-based room design and the reachability budget stay true, and it multiplies the hazard/enemy content (gnats and debris become footholds). Movement-first tuning: 0.8x damage, half poise, never consumes the riposte. **The Timber Hook (anchored rope grapple to authored beam glyphs) is the planned SECOND mechanic** for the tall-rooms milestone — spectacle traversal with no sequence-break risk, wanted only once rooms can be taller than 18 rows.
- 2026-07-18: **Typography and theme locked (Dustin delegated the call): Rajdhani** (OFL, licence in assets/fonts) as the single family — Medium body, Bold buttons/headers — via a project-wide theme (src/ui/theme.tres, gui/theme/custom). Buttons wear the mine's panel language with an amber focus ring doubling as the gamepad cursor. Rule going forward: scenes override sizes and accent colours only, never the font family.
- 2026-07-18: **DAILY DELVE + RECORDS shipped (M8 first half). Dustin's rules:** one ranked attempt per calendar day (spent at run START — no quit-refunds; later replays are practice), banked haul ranks with depth as tiebreak, and dailies are a LEVEL FIELD: heat 0, bare pickaxe, streak and session loadout untouched in both directions. Records screen ranks the run history (logging since round 4): today's daily, top-5 ranked dailies, top-5 free extractions, career line. Ghost replays + any online board remain M8's back half.
- 2026-07-18: **Daily ghosts shipped as POSITION TAPES** (room + position + facing at 20 Hz), not input logs — tiny, cannot desync, survive game updates; the GDD's input-resimulation ghosts remain a possible later upgrade for verification, not experience. The ghost rises only on PRACTICE runs of the recorded seed; the ranked attempt is deliberately unaccompanied. Tape holds exactly one run: today's ranked daily.
- 2026-07-18: **TIMBER HOOK + THE CHASM shipped.** The hook is skill_2 (E/RB): a straight zip to authored timber anchors ('o' glyphs) with an arrival pop; jump releases early keeping momentum; a hit cuts the rope. Zip-not-pendulum was deliberate — deterministic and shippable; a swing upgrade can replace the state later without touching anchors or bindings. Room HEIGHT is per-layout now (18 minimum): the Chasm (58x36) is the first tall room — zigzag tiers climbable both ways per the validator's law, anchors as the fast lane. Movement kit is now complete for v1: jump/roll/parry/pogo/hook.
- 2026-07-21 (claude.ai art session): **ENEMY IDENTITY SETTLED — the LOST CREWS.** The enemies are miners who never came back up: hollow, lantern-eyed, still carrying their tools. The woken-things/fauna direction was rejected. Closes the roster question the Setting section left open since 2026-07-15. Fits the fiction (the mine's casualties ARE its defenders), the visual language (lantern light against cold dark), and the greyscale/BodyJuice tint contract — a lantern-eyed silhouette reads through any telegraph colour.
- 2026-07-21 (claude.ai art session): **Player redesign direction: SMALL AND QUICK.** Compact Celeste-like proportions, big helmet, oversized pickaxe. Full character redesigns arrive via concept rounds in claude.ai sessions; production art stays in the ASCII sprite pipeline at the current canvas sizes, so the redesign is a frames-file change, not a pipeline change.
- 2026-07-22 (Dustin's call, in this session): **FTUE EVOLVES INTO A GUIDED FIRST DESCENT — "The First Descent."** The round-9 FTUE (verb signs sprinkled in the entry room + the gym for parry) is superseded by a purpose-built guided intro delve that teaches by DOING, one verb per station, in sequence: move → jump → attack (a passive dummy) → roll (a spike pit) → grapple-swing (a timber-anchor gap) → parry (one telegraphed grunt) → earn first ore → first shrine → a scripted capstone boss → extract your first haul into the real economy. Reuses the real Player/enemies/shrine/atmosphere/rooms; a new `TutorialDirector` replaces `RunCoordinator` so the extract/descend fork appears only at the finale (it is itself the last lesson). Teaching is diegetic — the WorldPrompt cards, not modal popups — matching the "up=out needs no tutorial" ethos. **Two forks Dustin picked:** (1) TEACH, DON'T WALL — the tricky verbs (parry, grapple) have escape hatches (roll past the blocker, a harder jump route past the gap) so no new player is trapped; the survival verbs are required by geometry anyway. (2) The capstone is a SCRIPTED, near-unlosable SET-PIECE (Asylum-Demon energy — "bosses are readable" over "bosses are hard"), not the tuned Overseer. First-run gated via a saved `intro_seen` flag; replayable from the menu; wiped by New Game. The gym remains as a practice space, no longer the primary parry teacher.
- 2026-07-22 (Dustin's call, in this session): **DIRECTIONAL VERBS ACCEPT STICK *AND* D-PAD — one honest "up/down".** The split Dustin flagged: `interact` was bound to D-pad up (button 11) while the directional verbs — extract, descend, enter-mine — were on `move_up`/`move_down` (left stick + keyboard only, NO D-pad). So on a pad "up" meant the D-pad for one thing and the stick for another, and the device-agnostic ▲/▼ prompt card couldn't reveal which — a player who learned "D-pad up = interact" tapped D-pad up at a cave mouth and nothing happened. Fix: `move_up` += D-pad up (11), `move_down` += D-pad down (12). Now stick, D-pad, and keyboard all drive up/down identically everywhere, so the single ▲/▼ arrow is honest for every device and no text card needs to distinguish D-pad from stick. Interact stays a button press (F / A-Cross / D-pad up) — deliberately NOT put on stick-up, which would auto-interact while walking. The `_deliberate` guard (stick needs ≥0.7 tilt; a D-pad *button* is always deliberate) already handled this correctly, and the "keys and D-pad are always deliberate" comments in run_coordinator/hub are now true rather than aspirational. **Real controller-button icon ART stays an M9 asset job** (the current text glyphs — A/B/LB/△ — are placeholder by prior decision, rounds 7–8); the recognizability win here is structural (one gesture, one meaning) rather than a new glyph set. Non-recommended alternatives Dustin passed over: drawing distinct D-pad-vs-stick shapes to *keep* the split, or dropping D-pad entirely. **Follow-on, same session:** playtest showed shops responded to D-pad up but NOT A/Cross — the round-8 "interact = A/Cross primary" decision had never actually landed in `project.godot` (interact was `F` + D-pad up only). Added A/Cross (button 0) to `interact`, restoring the intended primary engage button (re-introduces the blessed "A near a stall also hops" quirk). Also fixed the glyph: `hint_for` listed D-pad up first, so interact prompts showed a bare "↑" (reads as a direction, the exact "terrible icon" Dustin called out) — it now prefers a face/shoulder button over a D-pad direction, so interact shows A / ✕. **Refined further, same session (Dustin's call):** interact is now ENGAGE-BUTTON ONLY — `F` on keyboard, `A`/`Cross` on pad — with D-pad up REMOVED from interact entirely (keyboard has no "up" to interact, so the pad matches). Storefronts/options answer only interact, never "up"; the D-pad-glyph-preference tweak was reverted as unneeded once interact carries a single pad button. Directional verbs (extract/descend/enter-mine) keep stick + D-pad + keyboard. And the round-8 "A near a stall also hops" quirk is now KILLED in the hub: since interact and jump share A, the hub calls a new `Player.swallow_jump()` when a storefront press lands, dropping that tick's buffered jump so you engage without hopping. Scoped to the hub only — delves never call it, so jump stays fully live in a run (and pressing interact next to nothing in the hub still jumps normally). Known remaining: delve pickups/shrines (also on interact/A) still hop on grab; left as-is per Dustin's "delves can still jump" scope, easy to extend the same swallow if wanted.
- 2026-07-22 (Dustin's call, in this session): **ONBOARDING BOOKENDS THE MECHANICS TUTORIAL — story in, loop out.** Two problems: (1) the hub's **Train** post still routed to the old M2 gym (a "very old map"), not the new guided tutorial; (2) the tutorial teaches verbs but never states the *point* of the game — the fiction, or the descend-for-ore / spend-up-top loop. Fixes: **Train now routes to `tutorial_run.tscn`** (the death-proof, replayable "First Descent"); the gym still exists as a free-play practice space, just unwired from the button. A new **backstory crawl** (`intro_sequence.tscn`, `IntroSequence`) plays before the mechanics tutorial for fresh players — a skippable cinematic full-screen card sequence (6 beats, copy in an `@export` so it tunes in-inspector, unnamed per open question 5). Chosen cinematic-not-diegetic *because it is pure framing with no verb to teach* — the diegetic-card rule is about teaching. The title now routes fresh players to the crawl, which chains into the tutorial; the tutorial's finale still owns the `intro_seen` flag, so skipping/bailing correctly replays the whole arc. **The loop reveal is a first-visit HUB TOUR** (Dustin's pick over a narrative screen): on the first surface arrival the building WorldPrompts carry a title + one-line explanation — TRADE (permanent stacking upgrades), BLACKSMITH (buy/hone weapons), THE MINE (deeper pays richer, but bank before it keeps you — the greed pillar stated plainly) — reverting to the terse prompts the moment you first descend. Gated by a new saved `hub_toured` flag (wiped by New Game). Diegetic and reuses existing tech, per the ethos. **Intro length:** Dustin chose the richer 6-card crawl over a 3-card minimal.
- 2026-07-22 (Dustin's call, in this session): **AUDIO OVERHAUL — layered, event-complete, bus-mixed sound; still procedural.** The placeholder six-blip SFX and single-mood-per-descent music were a thin slice of the game's actual beats. Two forks Dustin picked: (1) **procedural overhaul NOW** over sourcing licensed audio — keeps the code-generated, reproducible, licence-free ethos (same as sprites/tileset/music), lands today, and leaves *recorded* audio as a later drop-in swap once the plumbing exists; (2) **rich, weapon+material-aware combat** over generic-but-layered. The Dead Cells trick: every connect plays a weapon-class **impact** one-shot (`&"pick"`/`&"blade"`/`&"blunt"`, rides on the `Hitbox` from the equipped weapon) stacked over the target's **material** one-shot (`&"flesh"`/`&"armor"`/`&"bone"`/`&"stone"`/`&"wood"`/`&"ecto"`, from `EnemyStats.material`) — so a Maul on the Brute's armour sounds nothing like the pickaxe on flesh, from 3+6 files instead of 18. `hit_landed` now carries `(damage, was_riposte, impact, material)`; a new `player_attacked(impact)` fires the swing whoosh on the wind-up (hit or miss). **~20 previously-silent events now sound:** poise break, enemy death (material-coloured), player death, all pickups (ore/heart/buff/weapon), upgrade purchase, shrine accept, plus the swinging-axe hazard (positional whoosh + first-contact chunk). **Music variety:** 3→5 delve moods, a dedicated title bed, and one-shot **extract** (hopeful lift) and **death** (sinking punish) stings — the emotional punctuation of the loop. **New systems:** a named **audio bus graph** (Master ← Music, SFX; UI + Ambience route through SFX; reverb only on Ambience) pinned by the check gate like the collision layers; **UI sound** wired globally and zero-touch via the viewport's focus changes + focus-gated accept/cancel (so a jump in the delve stays silent); a **mine wind ambient bed** under every delve; and the long-deferred **shrine hum** (positional, swells as you approach) — the roadmap's "needs an audio asset" is closed. Volume sliders now scale whole **buses** (Master/Music/SFX) rather than per-voice dB trims. `material` (EnemyStats) and `impact_profile` (WeaponData) are content-as-data — a new material/weapon-class is a new `.wav` and a field, no system edit. **Still M9's open call:** whether final audio stays this synthesised set or gets replaced by recorded/licensed foley + a composed track — the architecture makes that a swap, not a rebuild. Spare hooks baked but unwired for later: `doublejump`, `land_soft`, `ui_pause`.
- 2026-07-22 (Dustin's call, this session — Yvette's playtest: "levels feel much the same, repetitive"): **THE MINE HAS STRATA — three ZONES + a rest CAMP.** Diagnosis: the game had geometry variety (11 rooms) but zero *skin* variety — every depth wore the same grade, music and roster, so depth read as a number, not a journey. The Dead Cells lesson: areas feel different because they look/sound/fight different, not because rooms are cleverer. Fix, per Dustin's two forks (3 zones; recolor+atmosphere only, no new tile art): the run descends **THE UPPER WORKINGS** (the approved warm-timber look) → **THE HOT VEIN** (red grade, rising embers, a magma glow from below, heavier brute promotion — heat made visible) → **THE DEADLIGHT** (cold cyan, hanging spore-light, slinger/gnat lean — the eerie bottom). Zone order is FIXED (the descent is an arc, not a shuffle); which rooms fill each band stays seeded. A zone is a `ZoneData` resource (grade, tile tint, mote style, music pool, room pool, big-room pool, spawn lean) — content-as-data holds: a fourth zone is a .tres. **Guard-rails that are design, not implementation detail:** the player's lamp stays warm in every zone (warm = yours — the zones recolour the world AROUND that constant); enemy sprites are never tinted by zones (the greyscale telegraph contract survives); the exit beacon stays amber everywhere (amber = the way out); banding is pure arithmetic off the plan index so daily seeds pay nothing; every zone holds at least one combat room and each centrepiece lives in its home stratum (halls/undercroft/chasm — a slot-position bug nearly shipped the chasm out of existence, now test-pinned). **The CAMP:** a no-enemy rest room above the deep — helmet pile, campfire, ONE guaranteed heart (`camp_heart_heal`, an authored heal as a *place*, distinct from drop hearts) — pacing: after two strata of fights, one quiet warm room in the cold makes the last descent land. It never rains debris and never counts toward depth economy (the deep pays exactly as before). Zone title cards announce each stratum; the descend prompt names the next zone at a boundary — crossing should be felt at the door. melodic music (synth supercharged, not sourced) + combat-readability SFX.** Music read as "slow, repetitive, same"; Dustin chose to push the procedural generator rather than source real tracks (I'd researched CC0/commercial-OK options — Kenney, OpenGameArt CC0, Pixabay, itch.io — and offered a "you audition & pick, I integrate" path; recorded audio stays the eventual M9 swap). `gen_music.py` is now a real composer: hand-authored **melodies** (a sung lead voice — the biggest anti-repetition win), **verse/chorus** structure (two contrasting sections per track, not one looped 4-chord bed), and per-track timbre/energy (tempos to 142). Tracks ~doubled in length and actually develop. New SFX (Dustin's picks, all serving readability): a positional enemy **attack telegraph** (audible wind-up tell, pitched by attack reach — the Dead Cells "you can fight with your ears" idea), a positional **projectile whoosh** (flips up in pitch on a parry-reflect), and a low-health **heartbeat** (seamless loop under 30% HP). Wired the previously-spare `ui_pause` and `land_soft` (a short hop scuffs, a real drop thuds — `player_landed` now carries fall speed; new `player_health_changed` signal drives the heartbeat). No double-jump mechanic exists, so `doublejump.wav` stays a spare. Positional gameplay audio (telegraph, projectile, plus the earlier shrine hum and axe) lives on the node that makes it, NOT the Events→Sfx router — that router is for non-positional beats; a sound with a location belongs to its source.
- 2026-07-23 (Dustin's call, this session — "I get fully kitted and just one-shot everything"): **PAST THE FIFTH ROOM — the post-full-kit progression arc scoped.** Root cause, not a vibe: honing (in-run, weapon-scoped) is deliberately uncapped, but Mine Heat (cross-run, persistent) caps at 8 — two greed streaks built to race each other, except only one of them ever hits a wall. The fix is to uncap heat rather than cap honing, so both keep racing all the way down instead of honing winning by default. Four pieces, gated in order:
  1. **THE VEIN, once heat passes today's cap:** ships the already-flagged-but-unbuilt elite variants and depth-scaling enemy stats (see the Overseer entry, 2026-07-17). An elite is a modifier prefix on an existing enemy (candidates: Quickened, Armored, Volatile, Vampiric — roster not finalized) surfaced with a second persistent tint layered on the existing telegraph-tint pipeline, so it needs no new art — reuses `BodyJuice`.
  2. **THE THRESHOLD, a further heat streak PLUS a boss kill:** the gate into a second world is a COMBINATION unlock, not either alone — Varok (the Overseer), or a new deeper miniboss, plus a heat threshold, so the door only opens for someone both skilled enough to have won that fight and currently greedy enough to be at that heat when they do.
  3. **THE HOLLOW BELOW, a new zone chain past the Threshold:** what Varok was actually guarding. Reframe: the Overseer is a sentinel, not a treasure guardian — killing it breaches a containment, not a vault. Cold bioluminescent teal/violet, deliberately in contrast to the ember-lit strata above; a rising-tide hazard (periodic water-level surge forcing repositioning) is the zone's signature threat in place of debris rain; 2-3 new `EnemyStats` (working name the Drowned, plus one ranged/aquatic threat) rather than reskinned Brutes, since elites already own that job. Same `ZoneData`-resource architecture as the existing three strata — content-as-data holds; a fourth-plus zone chain is data files, not a system.
  4. **BLAZING, heat with no ceiling at all:** for players who've already seen everything above. Becomes the number Records tracks ("best heat survived") for the veteran chase — nearly free, since the Records screen and the underlying stat tracking already exist.

  Exact heat thresholds, which boss gates the Threshold, and the elite/Hollow-Below enemy rosters are explicitly NOT locked — ordinary tuning/content calls once this is built, same as every other exported value. Explicitly DEFERRED, not decided: reworking the vendor's three linear upgrades into mutually-exclusive build paths (working names Reckless / Bulwark / Wanderer) — revisit once the above has landed and it's clear build variety is still the gap. Also flagged as open threads needing their own session: an ascension currency for high-heat players, cosmetics/QoL spend only (the moment it buys stats it recreates the exact problem this scoping exists to fix), and a weekly rotating modifier delve reusing the shrine system's `active_modifiers` plumbing. None of this is built yet — design-then-code, per the project's usual order.
- 2026-07-23 (later, same session): **PAST THE FIFTH ROOM BUILT.** All four pieces above landed: `heat_vein_threshold`/`heat_threshold_gate` replace the old `heat_cap` in `game_state.gd`; `EliteModifierData` (Quickened/Armored/Volatile/Vampiric, `src/enemies/elites/`) rolls onto spawns past the Vein threshold with tint layered on `BodyJuice`; depth scaling is a second multiplier axis parallel to heat's; the Threshold reuses the mine mouth's otherwise-unused Interact press rather than new hub geometry — no new building, no new binding; the Hollow Below is a real single-zone `ZoneData` (`src/rooms/zones/hollow_below.tres`) reusing the existing room-shape and enemy-sprite pipeline (recolor + new stats, same precedent as the three surface zones — no new ASCII rooms or sprite frames), with two new enemies (the Drowned, the Drifter) and a `TideSurge` hazard (periodic full-room surge, unparryable/roll-dodgeable, same contract as spikes). Caught during final review before handoff: the deep room is a single shared `.tscn` with a hardcoded Varok marker, so the Hollow Below's finale was silently REFIGHTING him — straight contradiction of the Threshold's "never has to happen twice" promise. Fixed with `ZoneData.boss_swap`: a zone may override the deep room's authored "overseer" marker with a different kind (Hollow Below swaps to "drowned"); empty (default) leaves every other zone's Overseer exactly as authored. Explicitly a stopgap, not a real boss — no health bar, no fanfare; a proper Hollow Below finale boss is still the open thread the previous entry flagged. `delve_test.gd` gained coverage that walks an actual Hollow Below run to its deep room and asserts no `is_boss` enemy is there, so this does not silently regress. Full suite green (`check`, `loop_test`, `enemy_test`, `delve_test`, `rng_test`, `weapon_test`, `feel_test`).
- 2026-07-23 (next session, Dustin's playtest of the above): three notes. The tide mechanic landed well. Several rooms read as "the same rooms, reskinned" — several zones drawing from the same 7-room `MIDDLE_POOL` was always going to surface eventually. The Hollow Below's entrance was hard to find, buried as an Interact press at the ordinary mine mouth; Dustin's own fix — "different mine entrances... we've unlocked a new mine" — landed instead: a second opening (`HollowMouth`/`HollowMouthArt`/`HollowMarker` in `hub.tscn`) reusing the mine-mouth art cold-tinted, hidden until `threshold_open()`, taking the same deliberate DOWN press as the ordinary mine. Also fixed: `TideSurge.entry_grace_ticks` (240 ticks / 4s) — the zone-title card holds 3.45s with the room already live underneath, so a fresh Hollow Below room's tide was telegraphing AND surging once before a player could even see the room; the hazard now waits out the card before its cycle starts counting at all.
- 2026-07-23 (same session, **PILOT: COMPOSITE BIG ROOMS**, "let's start there and see"): the bigger ask behind the repetition note — Dustin wants genuinely bigger, more complex rooms with real route choices and puzzle potential, explicitly NOT via hand-authoring more individual whole rooms. Research (Dead Cells' concept-graph hybrid, Spelunky's solved-path-then-fill grid) plus close reading of this project's own generator found the key fact: UNDERCROFT and CHASM aren't built by any special system — they're just bigger hand-drawn ASCII rooms, same legend as everything else. So the opportunity isn't "make bigger rooms possible" (already true), it's combinatorial variety from composition. Landed, piloted on the Hot Vein's big-room slot: `tools/rooms/room_chunks.gd` (a library of 18-row ASCII fragments — 2 ENTRY, 2 EXIT, 4 MIDDLE, including `mezzanine`, which generalizes UNDERCROFT's shared-row two-tier trick — climb a tier ladder to a walkable roof, or just walk the floor below, both routes sharing the same entry/exit sockets) and `tools/rooms/room_stitcher.gd` (pure functions — no RNG, no file I/O — that concatenate chunk sequences row-by-row and exhaustively enumerate every valid combination; small library, so every shipped id is reviewable rather than random-sampled). `tools/gen_rooms.gd` merges the stitched composites into the SAME validate-then-build loop every hand-drawn room already goes through — `_validate`/`_check_reachability`/`_build` needed zero changes, confirmed by direct reading before writing anything. Entirely build-time/offline: `Delve` only ever does `load()` on a room id from a zone's pool and has no idea a `.tscn` was composed rather than hand-drawn, so zero runtime code changed. 8 chunks yielded 20 valid composites on the first full-library bake; 5 were curated into `hot_vein.tres`'s `big_pool` alongside the original `undercroft`. One safety gap closed proactively rather than deferred: `_check_reachability` only ever proved platforms reachable jumping UP from the floor, never that a spike-lined gap could be climbed back OUT of — safe today only by author discipline (GAP's own comment is a promise, not a machine check), and combinatorial assembly is exactly the scenario that breaks an unchecked promise like that. Added `_check_escapable` (~40 lines, reuses `_runs`/`_reachable_runs` unchanged) — passed against all 15 existing hand-drawn rooms with zero false positives, confirming the reasoning before it ever gated anything of Dustin's. Also closed a real pre-existing test gap: `delve_test.gd`'s walkability check only ever iterated the hardcoded `Delve.MIDDLE_POOL`/`BIG_POOL` constants, never the LIVE per-zone pools a `ZoneData` resource actually carries — meant nothing but the three flagship names ever got walked by the test suite. `zone_test.gd` now walks the union of every zone's live `room_pool` + `big_pool`. Explicitly deferred to a v2, not started: lock-and-key puzzle chunks — no switch/gate mechanic exists anywhere in the codebase today; would need new glyphs, new runtime scenes, an `Events`-based switch-to-gate link, and a second gate-aware validator pass (verify a key is reachable before its lock). v1 is pure stitched-geometry variety and route choice; v2 only after v1 has been played and earns it. Full suite green (`check`, all seven test scenes, plus the two new `zone_test` checks).
- 2026-07-23 (later, same session, Dustin looked at the composites in the editor before playing): **"just vague variations... nothing really."** Correct call, and a real miss, not a taste difference — checked the actual numbers: HALLS/UNDERCROFT are 116-120 columns, a normal middle room is 58. The v1 chunk sizes produced 40-72 column composites — SMALLER than an ordinary room in the bare entry+exit case, barely bigger than one even at best. A "big room" that's the same size as a small one is exactly a vague variation, whatever the internal structure. Fixed at the source rather than tuned around: every chunk in `room_chunks.gd` roughly doubled in width and enemy count (`combat_open` 28->44 cols/3->4 enemies, `mezzanine` 32->52 cols with its own shrine reward and a defender ON the roof route now, not just geometry), and `gen_rooms.gd`'s `middle_counts` changed from `[0, 1]` to `[1, 2]` — entry + two middles + exit is the new default shape, landing at 130-155 columns, past HALLS/UNDERCROFT rather than under a normal room. 64 valid composites from the same 8-chunk library (up from 20); `hot_vein.tres`'s `big_pool` re-curated to five 2-middle composites. One real bug caught in the process, worth remembering: hand-transcribing generated ASCII into the `.gd` file introduced a single off-by-one row-width typo that the validator caught immediately (row 14 of one composite, 53 chars against 52 everywhere else) — the fix was to stop transcribing by hand and have the generation script write the `.gd` file directly, the same discipline the room-authoring pipeline already uses ASCII-as-source-of-truth for. Full suite re-verified green, including `zone_test`'s walkability check over the newly-resized set (15 rooms).
- 2026-07-23 (later still, same session): **ENEMY CLUSTERING PASS, Dustin's call after comparing to Dead Cells.** Every enemy across the chunk library was an evenly-spaced single — no authored concept of a "pack," where Dead Cells deliberately clusters for burst-intensity beats between calmer stretches. Re-placed every middle chunk's enemies into read-able groups instead of solos: `combat_open` opens with a grunt pair, closes with a brute+dart pair; `mezzanine`'s lower lane now holds a genuine 3-enemy pack right at the base of the tier ladder (the floor route is an actual fight to weigh now, and the pack's chaos doubles as cover for slipping onto the ladder instead of fighting through it), its roof keeps one lone guard by the shrine; `combat_alcove` pairs a light opener with a solo Brute planted before the alcove (can't jump, so it can never reach the shrine — guards the approach, not the prize). Hard rule: never Brute+Brute in one cluster — two 90/70-poise swings overlapping isn't tense, it's a wall you cannot interrupt at all; a Brute pairs with at most one light add or stands alone as a checkpoint. Entry/exit chunks stayed light (a small adjacent pair, not a real cluster) — clustering is a middle-chunk beat, not the opener or closer. Caught and fixed before it shipped: the first pass of this edit dropped the `P` entry glyph from both entry chunks entirely while repositioning their enemies (an oversight, not a typo this time) — the validator's existing "exactly one P" check caught it on the very next bake, and the regeneration script now also asserts P/X presence per role before writing the file, on top of catching it structurally. Full suite re-verified green.
- 2026-07-23 (Dustin asked for a full pass to confirm everything looks great before playing): audited the entire 79-room catalog (15 hand-drawn + 64 composite) two ways. First, closed a real gap the P/X reachability check never covered: `_check_reachability` proved entry/exit markers stand on solid ground, but never enemy or shrine markers — every `g`/`b`/`d`/`E`/`S` glyph in every room, hand-drawn or composite, had only ever been proven correct by eye, the same story `_check_escapable` told about spike pits earlier this session. Extended the existing check to a `GROUNDED = "XPgbdES"` set (deliberately excluding `o`, the timber anchor, which floats by design, and `C`/`v`/`h`/`F`, whose placement patterns are already shipped and playtested — widening to them risked false-flagging real content rather than catching a real bug). Ran across all 79 rooms: zero failures — every hand-placed coordinate from this session's chunk work was actually correct, and nothing in the pre-existing hand-drawn set was ever ungrounded either. Second, a duplicate-position and spawn-crowding audit (do any two glyphs land on the same cell, silently overwriting one; are any two spawns close enough to visually stack on top of each other) — zero issues, every clustered pair/trio sits at least 3 columns apart. What this pass can and can't claim: it proves every room in the catalog is structurally sound — reachable, escapable, every marker grounded, nothing silently overwritten. It cannot judge feel — whether a cluster reads as exciting rather than just crowded is Dustin's call once he's actually played it, same as everything else in this project.
- 2026-07-23 (Dustin's first real playtest of the Threshold): reached the Hollow Below at last, two notes. **`heat_threshold_gate` lowered 12 -> 6** — "that took too long, other people would've gotten bored." Now sits below `heat_vein_threshold` (8), so a fast player can reach the Threshold before The Vein's elites ever show up in the ordinary mine — accepted, not fixed; revisit the ordering together if it reads wrong with more play. **The Hollow Below was paying almost no ore.** Checked the actual numbers before touching anything: the Drowned pays 4 haul (Grunt pays 3), the Drifter pays 3 (Dart pays 2) — swapping into Hollow Below's own enemies is a slight INCREASE per kill, not a decrease, and heat/depth multipliers apply identically, no special-casing broke them. The real cause: the Hollow Below is a shorter run (4 rooms, not 5), so it simply has less total content to loot — despite costing far more to reach (a permanent boss kill plus a live heat streak) than any ordinary zone. Added `ZoneData.ore_bonus` (new "Economy" export group, same pattern as `boss_swap`/`has_tide`), folded into `GameState.depth_haul_multiplier()` via a new `zone_ore_bonus` field that `Delve._advance()` sets every room load — same "GameState holds run state, Delve is the thing that knows the zone" pattern `depth` already uses, including the same reset in `end_run()`. Hollow Below set to `ore_bonus = 0.5`: a flat 50% ore premium for being the harder, more exclusive place, on top of depth and heat unchanged. `loop_test.gd` gained coverage: the multiplier actually multiplies, and `end_run()` actually resets it. Full suite green.
- 2026-07-23 (**THE KEYSTONE SYSTEM built**, the vendor build-path rework deferred back when Past the Fifth Room shipped, revisited now Dustin's actually played the rest): three forks, Dustin's calls. (1) **Pay-to-respec**, not permanent and not free — a bad early pick isn't a run-ruiner, but switching stays a real decision. (2) **No lock, just a bias** — every upgrade stays purchasable to its baseline cap regardless of active path; a keystone only lets ITS stat(s) keep going past baseline. Softer than the original "mutually-exclusive" framing, closer to today's system than a hard fork. (3) **Wanderer gets genuinely new mechanics** (extended roll i-frames + move speed), not a reweighting of the existing three stats — there was no mobility axis to reweight into.

  Landed as: the vendor's 3 upgrades become 4 (Damage, Armor, Health, **Mobility** — new, `src/systems/upgrades/mobility.tres`, +4% move speed and roll i-frame duration per level). Every upgrade keeps its baseline `max_level` (5) purchasable by anyone; `UpgradeData` gained `keystone_id` + `extended_max_level` (10) so a matching active keystone lets that stat keep going — Damage->Reckless, Armor+Health->Bulwark (a tank identity wants both defensive stats together), Mobility->Wanderer. `GameState.active_keystone` (persisted) plus `respec_keystone()`/`respec_cost()` carry the economy: free with nothing invested past baseline (whether that's because no keystone was ever picked, or one is active but nothing extended bought yet — caught by the test suite itself, the first draft only special-cased the literal first pick and charged full price for a genuinely-free-to-try second look), then `respec_base_cost (80) + respec_cost_per_level (60) * extended_levels_bought()` once there's real investment to walk away from. Explicitly not a hard wall — patient enough haul can still cycle every keystone eventually — the friction is meant to make "pick an identity" the felt experience, not a law.

  Mobility's roll i-frame extension is budgeted against the roll's OWN timing, not guessed: i-frames start at 75ms into a 350ms roll, leaving 275ms of room past the existing 200ms base duration — 10 extended levels at 7ms each lands at 270ms, safely inside budget, and `Player.effective_roll_iframe_duration_ms()` hard-clamps regardless so a future retune of any of those three numbers can't push i-frames past the roll ending. Vendor UI: a new keystone-picker row above the upgrade list, each stat's line shows `[Pathname]` next to its cap once it's been extended. Full suite green, including new coverage in both `loop_test.gd` (the economy: caps, cost scaling, persistence) and `weapon_test.gd` (the player mechanic: move speed rises, i-frames extend and stay clamped).
- 2026-07-23 (**THE STARTING CLASS LAYER built**, several rounds of back-and-forth, Dustin's calls throughout): the Keystone System evolved into something closer to a real class pick — a name, an instant bonus, a visible identity, gated behind a bit of progress. Landed as four pieces.

  **Renamed**: Reckless -> **Powderhand**, Bulwark -> **Ironback**, Wanderer -> **Tunnel Rat** — plain nouns tied to the mining fiction (a real historical/mining vocabulary: powder handlers, tunnel rats) rather than generic RPG archetypes, matching how everything else in this game is named.

  **Instant bonus on first pick**: picking a keystone now grants free levels immediately, not just a wider ceiling to grind into — Powderhand -> damage 0->4/10, Tunnel Rat -> mobility 0->4/10, Ironback -> armor 0->2/10 AND health 0->2/10 (splits its budget across both stats it governs rather than stacking on one, matching its already-bigger structural ceiling). New `GameState.KEYSTONE_BONUS_LEVELS` const and persisted `keystones_unlocked` dict, granted once ever per keystone per save via `maxi()` (never a downgrade for a player who'd already bought past the bonus floor). Anti-farming was the real design work here: without `keystones_unlocked`, a player could respec away and back — often free early, since `respec_cost()` is 0 with nothing invested — to farm the bonus repeatedly.

  **Visual identity — the weapon, not the player**: research surfaced a real constraint before any code got written — the player is explicitly never base-tinted, stated as a locked pillar in both CLAUDE.md and the GDD's own zone guard-rails ("the player's lamp stays warm in every zone... enemy sprites are never tinted by zones"). A naive "tint the player body per path" plan would have broken an established rule, not just a preference. Routed around it: `WeaponSprite` (`src/player/weapon_sprite.gd`) gained `_tinted_modulate()`, blending `GameState.KEYSTONE_COLOURS` into the weapon's existing `body.modulate`-derived flash colour at a modest `keystone_tint_strength` (0.3) — the weapon visibly recolors, in and out of combat, and the swing smear takes the full keystone colour on contact; the player body and lamp are untouched. The vendor's keystone buttons pick up the same colours as a font-colour accent, so the identity reads the same in the shop and on the character. Lamp/key-light recoloring was the other live lever found and explicitly left alone — the GDD guard-rail specifically locks it, and touching it deserves its own conversation, not a quiet side-effect of this one.

  **Progress gate**: no existing flag meant "has extracted at least once" — `total_runs` conflates extraction and death, `best_haul`/`mine_heat` both had false-negative edge cases. New `GameState.has_extracted`, same permanent-flag shape as `hub_toured`/`overseer_defeated`, set unconditionally in `extract()` (daily or free, either counts), never in `lose_run()`. The vendor's keystone row is hidden outright pre-extraction — same "built but invisible until earned" shape as the Hollow Below's hub entrance — and its status line doubles as the "this is new" moment once it appears (permanently elevated copy, not a toast), reusing that same precedent rather than building a new announcement system.

  Full suite green, including new `loop_test.gd` coverage for the bonus grant (single-stat, dual-stat, top-up-not-downgrade), the anti-farming guard (survives save/reload, not just in-memory), and `has_extracted`'s full lifecycle; new `weapon_test.gd` coverage confirming the tint actually shifts and matches the expected blend math. The vendor's visibility gating itself was verified end-to-end with a temporary check script (locked and unlocked states both instantiated and inspected) rather than left to manual-only verification.
- 2026-07-28 (Dustin's approval: "make it happen with what you think should be done"): **M8 COMPETITION LAYER — hardening landed, online day-board built, dormant until deployed.** The audit had found daily mode / ghosts / records already existed; what landed now is the remainder. **(1) Seed-derivation hardening:** `Rng.fnv32` (self-owned FNV-1a) replaces the engine's `hash()` in `daily_seed`, `seed_from_text`, AND `stream()` — Godot documents no cross-version stability for `hash()`, and streams ARE the delve layout, so an engine upgrade could have silently reshuffled every daily for players on mixed builds. Accepted one-time reshuffle of all prior seeds, taken NOW because no seed has ever been shared or ranked. `rng_test` now pins LITERAL derivation constants (939708641 for `daily:2026-07-28` etc.) — if those pins ever fail, seed derivation moved and it is a release blocker, not a test to update. **(2) The daily day is UTC everywhere** — seed, ranked-attempt gate (`today_string`), run-history timestamps, board rows all key off the same UTC string; the title's daily button now names the day (`DAILY DELVE · 2026-07-29`). **(3) Online day-board** (GDD competition model's "local first, online later" — later is now): `Leaderboard` autoload (registered before Settings, which stays last) posts the ranked daily's result and fetches the day's top rows. Stance, stated in code: the board is ADDITIVE UI — never blocks, never player-facing errors, fully offline-playable; failed submits park in `user://pending_score.json` and retry next boot. No accounts; a 12-char handle (Settings `daily`/`handle`, sanitized `[A-Za-z0-9_-]`, auto-minted `MINER-####`, editable under OPTIONS) is the only thing that leaves the machine. Records screen grows "THE DAY'S BOARD — EVERYONE" (top 5 + your rank) only when live. **Server is GitHub-native operationally, per Dustin's mid-session steer:** the Cloudflare Worker (`server/leaderboard/` — ~100 lines + D1, plausibility clamps only, honestly NOT anti-tamper) deploys via `.github/workflows/deploy-leaderboard.yml` on push, same contract as the existing Pages deploy — GitHub alone couldn't take the write path, since anonymous score submission through the GitHub API would mean shipping a token in the game. **Dormant by default:** `SERVICE_URL=""`; the launch switch is one commit after the README's 10-minute setup (D1 create + two repo secrets + paste URL).
- 2026-07-28 (Dustin's calls, publishing push session): **PAYDIRT locked as the name** (open question 5 answered — project name, export paths, bundle id `us.leapit.paydirt` all updated; the title screen already wore it). **Hit SFX de-tinned**: the "tin" Dustin flagged was the weapon-impact layer — `impact_pick` was a 2600/3900 Hz ring and `impact_blade` thin noise, all treble, no body; both reworked with the same recipe as the earlier armor fix (weight below 200 Hz, low-mid knock, brightness confined to a milliseconds-long transient), `mat_stone`'s 900/1500 ping pulled down to a low knock. `impact_blunt`, flesh/wood/bone, and especially the PARRY ring (a pillar — bright on purpose, legible eyes-shut) left untouched. **Real controller glyph art landed** (retires the rounds-7/8 placeholder note for chips): `Keybinds.hint_info()` returns structured chip info and `KeyChip.draw_action()` draws it — face buttons as round caps (Sony shapes as DRAWN geometry in Sony colours, tofu-proof, same rule as the ▲/▼ arrows; Xbox letters in official button colours, dimmed into the palette), D-pad as arrow caps, hold-to-commit becomes a radial sweep on round chips. Chip renderers only (PromptCard, KeyHint); sentence-style text hints stay text deliberately. New `tools/glyph_preview.tscn` proof-sheet rig. **App icon**: `tools/gen_icon.py` bakes `icon.png` (dark plate, amber border, faceted nugget — the KeyChip language at desktop size); project icon was previously unset (Godot robot).
- 2026-07-28 (Dustin's call: "the hub's artstyle is so different, and frankly worse, than the rest of the actual game"): **HUB OUTDOOR ART PASS — the surface finally went through the pipeline.** Diagnosis agreed in-session: the hub environment never got the art pass the delve got — a flat brown ColorRect box (background, floor, walls, the `_build_environment` pit-head shell) under a BRIGHT grade that hides nothing, with finished baked props sitting on M0 geometry. Fix reuses existing pipelines only, no new art tech: **(1) A real dusk sky** — gradient (night indigo to ember horizon), fixed-seed stars, a gradient moon, two sine+jitter silhouette ridgelines, and a distant pit-headframe silhouette on the horizon (the outpost belongs to a mining country now). **(2) The world tileset under your feet** — ground is `world_tileset.tres` rock with the delve's own variant mix and a mossy walk-line lip (65% moss vs the delve's 30 — the surface is the one overgrown place), and the hub's right end is a terraced rock face both mine mouths bore into. Before the Threshold opens, the Hollow's spot is genuinely solid rock, so the "appears where there was rock" reveal now reads literally. **(3) The pit-head gantry stays** — the old rock CEILING went (it read as underground; the hub is the surface), but the timber beam survives as an open-air gantry with full-height posts, and the hanging lanterns now start at its underside. All of it is `src/hub/hub_scenery.gd` building at runtime (MineAtmosphere precedent), VISUAL ONLY: tile layers run `collision_enabled = false` — the hub's Geometry StaticBody keeps owning collision, so this is a zero-gameplay-risk art change. Colours are authored pre-grade (the warm hub CanvasModulate multiplies them) and everything is exported for inspector tuning. Also new: `tools/screenshot.tscn` — a generic "instance any scene, settle, save PNG" rig (zone_shots' little sibling) used to verify this pass visually before handoff. Deferred to the dressing follow-up: scattered surface props on the new ground, silhouette detail (trees/buildings on the ridges), any grade retune Dustin wants after seeing the sky in motion.
